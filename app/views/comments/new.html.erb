<h2>Make a comment</h2>

<%= form_with(model: @comment, url: user_post_comments_path, local: true) do |form| %>
  <div>
    <%= form.label :body %><br>
    <%= form.text_area :body, rows: 6, cols: 35, required: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
